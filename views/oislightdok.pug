extends layout
include includes/parameterDocMixins

block content
  header.jumbotron.subhead
    div.container
      h1 OIS Light
      div.page-header
        p.lead Adressens relation til enheder, bygninger samt ejerskab er interessant i mange sammenhænge. Baseret på OIS data udstilles en række BBR ressourcer, som gør det muligt at etablere disse relationer. Hver ressource indeholder hovedsagligt kun ressourcens id, samt identer til relaterede ressourcer, hvilket er tilstrækkeligt til at danne relationer mellem adresser og BBR ressourcerne. BBR rummer mange andre data, som ikke udstilles af DAWA. Det skyldes, at al anvendelse skal være i medfør af § 6, stk. 1, nr. 7, i persondataloven, hvorefter behandlingen er nødvendig for, at den dataansvarlige eller den tredjemand, til hvem oplysningerne videregives, kan forfølge en berettiget interesse og hensynet til den registrerede ikke overstiger denne interesse. Link til persondataloven: https://www.retsinformation.dk/Forms/R0710.aspx?id=828 

    div.container
      div.row
        div.span3
          ul#indhold.nav.nav-list
            li(data-toggle='collapse', data-target='#ois').nav-header OIS API
              ul#ois.nav.nav-list
                each oisEntityName in docUtil.getOisEntityNames()
                  li
                    a(href='#ois_' + oisEntityName) #{docUtil.getOisPlural(oisEntityName)}

        div.span9
          section
            each oisEntityName in docUtil.getOisEntityNames()
              h3.anchor-header(id='ois_' + oisEntityName) OIS #{docUtil.getOisPlural(oisEntityName)} søgning
              +documentOisQueryResource(oisEntityName, 'public')
              +documentOisGetByKeyResource(oisEntityName, 'public')
block append scripts
